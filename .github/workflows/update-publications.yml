name: Actualizar publicaciones ORCID

on:
  schedule:
    - cron: "0 9 * * 1"
      timezone: "Europe/Madrid"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  actualizar-publicaciones:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar el repositorio
        uses: actions/checkout@v6

      - name: Instalar Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests

      - name: Obtener token de ORCID
        env:
          ORCID_CLIENT_ID: ${{ secrets.ORCID_CLIENT_ID }}
          ORCID_CLIENT_SECRET: ${{ secrets.ORCID_CLIENT_SECRET }}
        run: |
          python - <<'PY'
          import os
          import requests

          response = requests.post(
              "https://orcid.org/oauth/token",
              headers={"Accept": "application/json"},
              data={
                  "client_id": os.environ["ORCID_CLIENT_ID"],
                  "client_secret": os.environ["ORCID_CLIENT_SECRET"],
                  "grant_type": "client_credentials",
                  "scope": "/read-public",
              },
              timeout=60,
          )

          response.raise_for_status()
          token = response.json()["access_token"]

          with open(os.environ["GITHUB_ENV"], "a") as file:
              file.write(f"ORCID_ACCESS_TOKEN={token}\n")
          PY

      - name: Ejecutar programa
        run: python orcid_publications.py

      - name: Guardar archivos generados
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add publications.json publications.html publications.bib

          if ! git diff --cached --quiet; then
            git commit -m "Actualizar publicaciones ORCID"
            git push
          else
            echo "No hay cambios que guardar."
          fi
